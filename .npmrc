# pnpm 配置文件
# 使用 pnpm 作为包管理器

# 自动安装 peer dependencies
auto-install-peers=true

# 严格 peer dependencies（可选）
# strict-peer-dependencies=true

# 使用符号链接（pnpm 默认，更快）
# node-linker=isolated

# 提升依赖（可选，减少重复）
# shamefully-hoist=false

