# 测试总结与反思界面逻辑分析

## 一、界面概述

**界面ID**: `post-test-view`  
**显示时机**: 所有10张Rorschach图片测试完成后  
**主要功能**: 通过一系列问题收集用户对图片的主观评价和选择

---

## 二、核心数据结构

### 2.1 问题列表 (POST_TEST_QUESTIONS)

定义位置: `script/appState.js` (第121-168行)

问题列表包含以下问题：

1. **represent** - "选择一张最能代表你自己的图片"
   - **represent_why** - "请解释一下为什么" (why问题，不显示在界面)

2. **father** - "选择一张最能代表你父亲的图片"
   - **father_why** - "请解释一下原因" (why问题，不显示在界面)

3. **mother** - "选择一张最能代表你母亲的图片"
   - **mother_why** - "请解释一下原因" (why问题，不显示在界面)

4. **like** - "你最喜欢哪一张图片"
   - **like_why** - "为什么呢？" (why问题，不显示在界面)

5. **dislike** - "你最讨厌哪一张图片"
   - **dislike_why** - "请解释一下原因" (why问题，不显示在界面)

6. **mood** - "测完之后，相比刚开始测试时，你现在心情如何？"

**关键特性**:
- `why`类型问题（以`_why`结尾）不会在界面上显示，但会通过TTS语音播报
- 只有非`why`问题会显示在界面上，要求用户选择图片

### 2.2 答案存储

**存储位置**: `state.postTestAnswers` (对象)

**数据结构**:
```javascript
{
  represent: 3,      // 用户选择的图片编号 (1-10)
  father: 5,
  mother: 2,
  like: 8,
  dislike: 4,
  mood: 7
}
```

---

## 三、界面初始化流程

### 3.1 入口函数: `showPostTestView()`

**位置**: `script/appMain.js` (第2858-2927行)

**执行步骤**:

1. **状态设置**
   ```javascript
   state.stage = "post"  // 设置测试阶段为"后测试"
   ```

2. **UI元素隐藏/显示**
   - 隐藏: `mainContent`, `controlsBar`, `image-container`, `blackhole-bg-container`
   - 显示: `post-test-view`
   - 隐藏字幕管理器
   - 移除`test-mode`类
   - 隐藏能量柱容器

3. **重置界面状态**
   - 清空`post-test-grid`内容
   - 隐藏`finish-btn`按钮
   - 清空`question-text`文本

4. **生成图片网格**
   ```javascript
   for (let i = 0; i < state.totalImages; i++) {
     // 创建10个图片项，每个包含：
     // - 图片元素 (rorschach-blot-{i+1}.webp)
     // - 标签 ("图 {i+1}")
     // - 点击事件监听器 (handleImageSelection)
   }
   ```

5. **初始化问题索引**
   ```javascript
   currentQuestionIndex = restoredSnapshot?.payload?.currentQuestionIndex ?? 0
   ```

6. **开始第一个问题**
   ```javascript
   askNextQuestion()  // 显示并播报第一个问题
   ```

---

## 四、问题显示与播报流程

### 4.1 核心函数: `askNextQuestion()`

**位置**: `script/appMain.js` (第2929-3082行)

**执行流程**:

#### 步骤1: 清除之前的选择状态
```javascript
document.querySelectorAll(".grid-item.selected")
  .forEach((el) => el.classList.remove("selected"))
```

#### 步骤2: 查找下一个应该显示的问题
```javascript
let question = null
while (currentQuestionIndex < POST_TEST_QUESTIONS.length) {
  const candidate = POST_TEST_QUESTIONS[currentQuestionIndex]
  if (shouldDisplayQuestion(candidate)) {  // 跳过why问题
    question = candidate
    break
  }
  currentQuestionIndex++
}
```

**`shouldDisplayQuestion()`函数**:
- 位置: `script/appState.js` (第194-196行)
- 逻辑: 过滤掉所有以`_why`结尾的问题

#### 步骤3: 判断是否所有问题已完成

如果所有问题都已完成：
- 隐藏问题文本和图片网格
- 显示`finish-btn`按钮（初始为禁用状态）
- 播报结束文案: "好的，再次感谢您的时间..."
- 播报完成后启用按钮

#### 步骤4: 显示主问题

如果还有问题：
- 设置`questionText.textContent = question.text`
- 禁用图片网格交互: `gridContainer.style.pointerEvents = "none"`

#### 步骤5: TTS语音播报

**主问题播报**:
```javascript
const ttsQuery = buildTTSQuery(question.text)
await sendTextQuery(ttsQuery, { ensure: false })
// 等待播报完成（估算时间：每字约250ms）
```

**Why问题播报** (如果存在):
- 查找对应的why问题: `findWhyQuestion(question.key)`
- 等待主问题音频播放完成
- 播报why问题文本
- Why问题不会显示在界面上，只通过语音播报

#### 步骤6: 启用图片选择
```javascript
gridContainer.style.pointerEvents = "auto"  // 允许用户点击图片
```

---

## 五、图片选择处理流程

### 5.1 核心函数: `handleImageSelection()`

**位置**: `script/appMain.js` (第3084-3107行)

**触发时机**: 用户点击图片网格中的任意图片

**执行流程**:

1. **获取选择的图片索引**
   ```javascript
   const selectedIndex = event.currentTarget.dataset.index  // 0-9
   const questionKey = POST_TEST_QUESTIONS[currentQuestionIndex].key
   ```

2. **保存答案**
   ```javascript
   state.postTestAnswers[questionKey] = parseInt(selectedIndex) + 1  // 转换为1-10
   ```

3. **更新UI选中状态**
   ```javascript
   // 清除所有选中状态
   document.querySelectorAll(".grid-item.selected")
     .forEach((el) => el.classList.remove("selected"))
   // 标记当前选中项
   event.currentTarget.classList.add("selected")
   ```

4. **移动到下一个问题**
   ```javascript
   currentQuestionIndex++
   // 跳过why问题，找到下一个应该显示的问题
   while (
     currentQuestionIndex < POST_TEST_QUESTIONS.length &&
     !shouldDisplayQuestion(POST_TEST_QUESTIONS[currentQuestionIndex])
   ) {
     currentQuestionIndex++
   }
   ```

5. **保存会话快照**
   ```javascript
   saveSessionSnapshot("post_test")
   ```

6. **禁用交互并继续下一个问题**
   ```javascript
   document.getElementById("post-test-grid").style.pointerEvents = "none"
   setTimeout(askNextQuestion, 500)  // 500ms后显示下一个问题
   ```

---

## 六、完成与提交流程

### 6.1 核心函数: `finishAndSave()`

**位置**: `script/appMain.js` (第3110-3216行)

**触发时机**: 用户点击"提交"按钮（所有问题回答完成后）

**执行流程**:

1. **安全检查**
   ```javascript
   if (finishBtn && finishBtn.disabled) {
     return  // 防止在播报期间点击
   }
   ```

2. **断开TTS连接**
   ```javascript
   if (window.dialogClient) {
     window.dialogClient.disconnect()
   }
   ```

3. **停止音频录制**
   ```javascript
   if (state.mediaRecorder && state.mediaRecorder.state === "recording") {
     state.mediaRecorder.stop()
   }
   ```

4. **导出交互追踪数据**
   - 停止交互追踪
   - 导出JSON数据（包含统计信息、元数据）
   - 记录旋转次数、画笔轨迹、音频时间戳等

5. **提交数据到服务器** (异步)
   ```javascript
   await window.submitTestDataToServer(
     window.InteractionTracker,
     audioBlob,
     state.postTestAnswers  // 包含所有后测试答案
   )
   ```

6. **显示汇总页面**
   ```javascript
   showSummary({ reportStatus: { ...DEFAULT_REPORT_WAITING_STATUS } })
   ```

---

## 七、关键状态变量

### 7.1 全局变量

- **`currentQuestionIndex`**: 当前问题索引 (0-11)
  - 位置: `script/appMain.js` (第69行)
  - 用途: 跟踪当前显示到哪个问题

- **`state.postTestAnswers`**: 存储所有后测试答案
  - 结构: `{ questionKey: imageNumber }`
  - 初始值: `{}`

- **`state.stage`**: 测试阶段标识
  - 值: `"post"` (后测试阶段)

### 7.2 UI元素引用

- **`postTestView`**: 后测试视图容器
- **`questionText`**: 问题文本显示元素
- **`post-test-grid`**: 图片网格容器
- **`finishBtn`**: 提交按钮

---

## 八、问题过滤逻辑

### 8.1 Why问题处理机制

**设计目的**: Why问题通过语音播报，不显示在界面上，避免用户混淆

**实现方式**:

1. **问题定义**: Why问题以`_why`结尾
   ```javascript
   { key: "represent_why", text: "请解释一下为什么..." }
   ```

2. **过滤函数**: `shouldDisplayQuestion()`
   ```javascript
   export function shouldDisplayQuestion(question) {
     return !isWhyQuestion(question.key)  // 过滤why问题
   }
   ```

3. **Why问题播报**: 在`askNextQuestion()`中，主问题播报完成后，查找并播报对应的why问题
   ```javascript
   const whyQuestion = findWhyQuestion(question.key)
   if (whyQuestion) {
     // 播报why问题（不显示在界面）
   }
   ```

---

## 九、交互流程时序图

```
用户完成10张图片测试
    ↓
showPostTestView()
    ↓
生成10张图片网格
    ↓
askNextQuestion() - 显示第一个问题
    ↓
TTS播报主问题 + Why问题
    ↓
启用图片选择 (pointerEvents = "auto")
    ↓
[用户点击图片]
    ↓
handleImageSelection()
    ↓
保存答案到 state.postTestAnswers
    ↓
currentQuestionIndex++ (跳过why问题)
    ↓
askNextQuestion() - 显示下一个问题
    ↓
... (重复直到所有问题完成)
    ↓
显示"提交"按钮
    ↓
[用户点击提交]
    ↓
finishAndSave()
    ↓
提交数据到服务器
    ↓
showSummary() - 显示汇总页面
```

---

## 十、关键代码位置索引

| 功能 | 文件 | 行号范围 |
|------|------|----------|
| 问题列表定义 | `script/appState.js` | 121-168 |
| 问题过滤函数 | `script/appState.js` | 194-196 |
| 界面初始化 | `script/appMain.js` | 2858-2927 |
| 问题显示逻辑 | `script/appMain.js` | 2929-3082 |
| 图片选择处理 | `script/appMain.js` | 3084-3107 |
| 完成提交逻辑 | `script/appMain.js` | 3110-3216 |
| 界面HTML结构 | `index.html` | 241-248 |
| 界面样式定义 | `css/app.css` | 1430-2205 |

---

## 十一、注意事项

1. **Why问题不显示**: Why问题只通过TTS播报，不会显示在界面上
2. **图片编号转换**: 界面使用0-9索引，存储时转换为1-10
3. **交互禁用机制**: 在问题播报期间，图片网格会被禁用，防止用户提前选择
4. **会话快照**: 每次选择后都会保存会话快照，支持断点续测
5. **按钮状态管理**: 完成按钮在TTS播报期间会被禁用，防止误操作
6. **异步提交**: 数据提交是异步进行的，不会阻塞界面显示

---

## 十二、数据流向

```
用户选择图片
    ↓
handleImageSelection()
    ↓
state.postTestAnswers[questionKey] = imageNumber
    ↓
saveSessionSnapshot("post_test")
    ↓
[所有问题完成后]
    ↓
finishAndSave()
    ↓
window.submitTestDataToServer(
  InteractionTracker,
  audioBlob,
  state.postTestAnswers  ← 后测试答案数据
)
    ↓
服务器API
```

---

## 总结

测试总结与反思界面是一个**问答式交互界面**，通过**语音播报 + 图片选择**的方式收集用户对Rorschach图片的主观评价。核心特点包括：

1. **问题-答案配对**: 每个主问题对应一个why问题（语音播报）
2. **渐进式交互**: 一个问题回答完成后才显示下一个问题
3. **状态持久化**: 支持会话保存和恢复
4. **数据完整性**: 收集所有答案后统一提交到服务器



